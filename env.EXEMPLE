# Adjust vars and save a version of me as ".env"
MONGO_PORT=27017
MONGODB_URI=mongodb://mongodb:${MONGO_PORT}/bite_swipe

FRONTEND_PORT=4200
FRONTEND_URL=http://localhost:${FRONTEND_PORT}

ALLOWED_ORIGINS=${FRONTEND_URL}

BACKEND_PORT=3000
BACKEND_URL=http://localhost:${BACKEND_PORT}
